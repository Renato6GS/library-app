<section class="form-container">
  <h3 class="form-title">Queremos seguir mejorando y por eso nos gustaría leerte</h3>
  <p>Déjanos un comentario 😀</p>
  <form id="commentsForm" class="form">
    <div class="first-part-form">
      <div class="username-container">
        <label for="username">Nombre</label>
        <input type="text" id="username" name="username" value="" placeholder="John Doe" required>
      </div>
      <div class="email-container">
        <label for="email">Email</label>
        <input type="email" id="email" name="email" placeholder="johndoe@gmail.com" required>
      </div>
    </div>
    <div class="comment-container">
      <label class="label-comment" for="comment">Comentario</label>
      <textarea name="comment" id="comment" cols="30" rows="4" placeholder="Me encatan tu página..."
        required></textarea>
    </div>
    <button class="btn-submit" type="submit">Enviar</button>
  </form>
</section>

<style>
  .form-container {
    margin: 100px auto;
    width: 100%;
    max-width: 1220px;
  }

  .form-title {
    font-family: 'Lato Bold', Arial, Helvetica, sans-serif;
    font-size: 1.5rem;
    color: var(--black);
    width: 100%;
    max-width: 400px;
    line-height: 37px;
  }

  p {
    font-family: 'Lato', Arial, Helvetica, sans-serif;
    font-size: 1.1rem;
    color: var(--light-black)
  }

  .form {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .first-part-form {
    display: flex;
    justify-content: space-between;
    gap: 1.5rem;
  }

  .username-container,
  .email-container,
  .comment-container {
    width: 100%;
    position: relative;
  }

  input,
  textarea {
    width: 100%;
    padding: 27px 13px 7px;
    font-size: 1rem;
    font-family: 'Lato', Arial, Helvetica, sans-serif;
    background-color: var(--ligth-gray);
    border: 1px solid var(--gray);
    border-radius: 5px;
    outline: none;
  }

  input:focus,
  textarea:focus {
    border-color: var(--black);
  }

  label {
    position: absolute;
    left: 2.5%;
    top: 7%;
    color: var(--light-black);
    font-family: 'Lato Bold', Arial, Helvetica, sans-serif;
    font-size: .8rem;
  }

  .label-comment {
    left: 1.1%;
  }

  .input-comment {
    height: 100px;
  }

  textarea {
    resize: none;
  }

  .btn-submit {
    width: 100%;
    max-width: 100px;
    color: var(--white);
    font-family: 'Lato', Arial, Helvetica, sans-serif;
    padding: 15px 12px;
    margin: 0 auto;
    background-color: var(--primary-color);
    border: none;
    border-radius: 4px;
    outline: none;
    cursor: pointer;
  }
</style>